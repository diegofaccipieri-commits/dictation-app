import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("DictationApp")
                .font(.headline)
            Text("Ready")
                .foregroundColor(.secondary)
        }
        .padding()
        .frame(width: 360, height: 240)
    }
}
